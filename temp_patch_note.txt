patching nav h2 styles

Full project explanation and summary:

What changed:
- The main accent color has been changed from red to blue across the site. This includes the button background, hover effects, shadows, and section highlight elements.
- The About section now uses a stronger, more visible blue-white radial background. It also has a cleaner border, a larger title style, and better contrast for paragraph text.
- Today's Specials section was enhanced with its own blue-themed section background, stronger border, and more clearly styled drink cards. This makes the specials stand out as a separate visual block.
- Existing page structure and navigation were preserved. The homepage hero, menu layout, and contact content remain in place while the visual theme was refined.

Why it was changed:
- The user requested a different color than the previous reddish/purple tone, so the accent palette was updated to blue to better reflect the requested style.
- The About section needed to stand out more from the page background, so the styling was made brighter and more distinct.
- Today's Specials were originally too soft and blended into the page. The new design gives them a unique background and stronger card contrast so they are more visible.

Files affected:
- `css/style.css`: changed color variables and accent rules, updated `.btn`, `.visit-us`, `.about`, `.featured`, and card display styles.
- `temp_patch_note.txt`: added this explanatory summary.

Current status:
- Visual improvements are in place for color, contrast, and section separation.
- The site now uses a consistent blue accent palette.
- The About and Today's Specials sections are more clearly defined visually.
- Further updates can include matching text accents and card labels if desired.

Note file renamed to `note.md` and created in the project root.
- `note.md` contains the full folder summary, what was changed, how the changes were applied, and the current project state.